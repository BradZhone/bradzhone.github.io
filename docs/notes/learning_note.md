# Learning Note

## 1 性能指标

- QPS（queries per second）：每秒查询数，查询操作
- TPS（transactions per second）：每秒事务数，增删改操作
- OPS（operations per second）：每秒操作数
- RT（response time）：请求响应时间
- QPS类似TPS，但一个请求事务可能产生多次查询操作
- Load Factor： 负载因子，哈希表中存入的元素与容量大小的比值

 